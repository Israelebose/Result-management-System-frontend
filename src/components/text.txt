import React from 'react';

const StudentDashboard = () => {
  // Dummy data simulating database values
  const studentData = {
    id: "S12345",
    name: "John Doe",
    email: "john.doe@university.edu",
    registrationNumber: "2023/123456",
    level: "300",
  };

  const enrolledCourses = [
    { code: "CSC101", title: "Introduction to Computer Science", units: 3 },
    { code: "MAT201", title: "Calculus II", units: 4 },
    { code: "ENG102", title: "English Composition", units: 2 },
  ];

  const grades = [
    { course: "CSC101", score: 85, grade: "B" },
    { course: "MAT201", score: 72, grade: "C" },
    { course: "ENG102", score: 90, grade: "A" },
  ];

  const announcements = [
    { id: 1, title: "Exam Schedule Released", date: "2025-08-20", details: "Exams start next week. Check your schedule." },
    { id: 2, title: "Library Hours Extended", date: "2025-08-19", details: "Library now open until 10 PM." },
  ];

  return (
    <div className="bg-gradient-to-br from-gray-900 to-gray-800 text-white p-6 sm:p-8 lg:p-10 min-h-[calc(100vh-64px)]">
      {/* Header Section */}
      <div className="mb-8">
        <h1 className="text-3xl sm:text-4xl font-bold bg-gradient-to-r from-blue-400 to-purple-600 bg-clip-text text-transparent">
          Student Dashboard
        </h1>
        <p className="text-gray-400 mt-2">Welcome, {studentData.name}! Manage your academic journey here.</p>
      </div>

      {/* Dashboard Grid */}
      <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
        {/* Profile Card */}
        <div className="lg:col-span-1 bg-gray-800/50 backdrop-blur-md rounded-xl p-4 sm:p-6 shadow-lg hover:shadow-xl transition-shadow duration-300">
          <h2 className="text-xl sm:text-2xl font-semibold mb-4 border-b border-gray-700 pb-2">Profile</h2>
          <div className="space-y-2">
            <p className="text-gray-300"><strong>ID:</strong> {studentData.id}</p>
            <p className="text-gray-300"><strong>Name:</strong> {studentData.name}</p>
            <p className="text-gray-300"><strong>Email:</strong> {studentData.email}</p>
            <p className="text-gray-300"><strong>Reg. Number:</strong> {studentData.registrationNumber}</p>
            <p className="text-gray-300"><strong>Level:</strong> {studentData.level}</p>
          </div>
        </div>

        {/* Courses Card */}
        <div className="lg:col-span-2 bg-gray-800/50 backdrop-blur-md rounded-xl p-4 sm:p-6 shadow-lg hover:shadow-xl transition-shadow duration-300">
          <h2 className="text-xl sm:text-2xl font-semibold mb-4 border-b border-gray-700 pb-2">Enrolled Courses</h2>
          <div className="overflow-x-auto">
            <table className="w-full text-left">
              <thead>
                <tr className="bg-gray-700/50">
                  <th className="p-2 text-gray-300">Code</th>
                  <th className="p-2 text-gray-300">Title</th>
                  <th className="p-2 text-gray-300">Units</th>
                </tr>
              </thead>
              <tbody>
                {enrolledCourses.map((course, index) => (
                  <tr key={index} className="border-b border-gray-700/50 hover:bg-gray-700/30 transition-colors">
                    <td className="p-2">{course.code}</td>
                    <td className="p-2">{course.title}</td>
                    <td className="p-2">{course.units}</td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </div>

        {/* Grades Card */}
        <div className="lg:col-span-2 bg-gray-800/50 backdrop-blur-md rounded-xl p-4 sm:p-6 shadow-lg hover:shadow-xl transition-shadow duration-300">
          <h2 className="text-xl sm:text-2xl font-semibold mb-4 border-b border-gray-700 pb-2">Grades</h2>
          <div className="overflow-x-auto">
            <table className="w-full text-left">
              <thead>
                <tr className="bg-gray-700/50">
                  <th className="p-2 text-gray-300">Course</th>
                  <th className="p-2 text-gray-300">Score</th>
                  <th className="p-2 text-gray-300">Grade</th>
                </tr>
              </thead>
              <tbody>
                {grades.map((grade, index) => (
                  <tr key={index} className="border-b border-gray-700/50 hover:bg-gray-700/30 transition-colors">
                    <td className="p-2">{grade.course}</td>
                    <td className="p-2">{grade.score}</td>
                    <td className="p-2">{grade.grade}</td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </div>

        {/* Announcements Card */}
        <div className="lg:col-span-1 bg-gray-800/50 backdrop-blur-md rounded-xl p-4 sm:p-6 shadow-lg hover:shadow-xl transition-shadow duration-300">
          <h2 className="text-xl sm:text-2xl font-semibold mb-4 border-b border-gray-700 pb-2">Announcements</h2>
          {announcements.length > 0 ? (
            <div className="space-y-4">
              {announcements.map((announcement) => (
                <div key={announcement.id} className="p-3 bg-gray-700/30 rounded-lg hover:bg-gray-700/50 transition-colors">
                  <h3 className="font-medium">{announcement.title}</h3>
                  <p className="text-sm text-gray-400">{announcement.date}</p>
                  <p className="text-sm text-gray-300 mt-1">{announcement.details}</p>
                </div>
              ))}
            </div>
          ) : (
            <p className="text-gray-400">No announcements found.</p>
          )}
        </div>
      </div>
    </div>
  );
};

export default StudentDashboard;




 {/* Announcements Section */}
        <div className="bg-white dark:bg-gray-800 rounded-xl shadow-lg p-6 ml-2 mr-2 mb-8">
          <h3 className="text-xl font-semibold mb-4 text-textColor1">
            Recent Announcements
          </h3>
          {announcements.length ? (
            <div className="space-y-3 overflow-y-auto no-scrollbar h-[45vh] sm:h-[18vh]">
              {announcements.map((a) => (
                <div
                  key={a.id}
                  className="p-3 border border-gray-400 rounded-lg dark:border-gray-700 flex flex-col sm:flex-row sm:items-center gap-4 sm:gap-10 cursor-pointer"
                  onClick={() => setSelectedAnnouncement(a)}
                >
                  <div className="flex items-center sm:w-3/12 text-textColor1 dark:text-textColor1 whitespace-nowrap">
                    <h2
                      className={`content-center text-center text-xl bg-gray-700 circle w-10 h-10 font-medium text-white transition-opacity duration-300 flex-shrink-0`}
                    >
                      {a.user?.firstName?.slice(0, 1).toUpperCase() || "U"}
                      {a.user?.lastName?.slice(0, 1).toUpperCase() || "N"}
                    </h2>
                    <div className="flex flex-col ml-3 transition-opacity duration-300 whitespace-nowrap">
                      <span className="">
                        {a.user?.firstName || "Unknown"}{" "}
                        {a.user?.lastName || "User"}
                      </span>
                      <small>{a.user?.role || "Admin"}</small>
                    </div>
                  </div>

                  <div className="flex flex-col sm:items-center justify-around sm:flex-row sm:w-10/12 gap-2">
                    <div className="space-y-1.5">
                      <p className="font-semibold">{a.title}</p>
                      <p className="text-sm text-gray-600 dark:text-gray-400">
                        {a.message.substring(0, 100)}...{" "}
                        {/* Preview truncate */}
                      </p>
                    </div>
                    <div>
                      <p className="text-xs text-gray-500 mt-1">
                        {moment(a.createdAt).format("MMM D, YYYY h:mm A")}
                      </p>
                    </div>
                  </div>
                </div>
              ))}
            </div>
          ) : (
            <p>No announcements found.</p>
          )}
        </div>





